{
  "name": "gpt-elevation-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build",
    "install": "npm install --legacy-peer-deps"
  }
}